{
  "id": "manager_development_integration",
  "title": "Manager development integration",
  "steps": [
    {
      "id": "merge_feature_branch",
      "title": "Run `bun run agent:merge-feature -- <feature-branch-name>` to refresh the worker feature branch from latest `origin/development`, stop for worker conflict resolution if needed, create a dedicated manager integration worktree, merge the feature branch there, and push `development` to origin"
    }
  ]
}
