{
  "name": "@agent-infrastructure/dashboard-terminal-server",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun x tsgo --project tsconfig.json --noEmit",
    "check": "bun x tsgo --project tsconfig.json --noEmit",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "bun-types": "^1.3.10",
    "typescript": "^5.4.5"
  }
}
