{
  "id": "work_at_path_exists",
  "title": "Required worker path exists",
  "description": "Checks that one required file system path exists on the selected work-at target.",
  "execution": {
    "kind": "work-at",
    "workTargetParam": "workTarget"
  },
  "command": ["bash", "-lc", "test -e {{path}}"],
  "timeoutMs": 3000
}
